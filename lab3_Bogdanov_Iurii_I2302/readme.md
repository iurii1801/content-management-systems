# Лабораторная работа №3. Разработка простой темы WordPress

## Цель работы

Научиться создавать собственную тему WordPress, разобраться в её минимальной структуре и принципах работы шаблонов.

## Инструкция по запуску

1. Установить WordPress.
2. Скопировать папку темы в wp-content/themes.
3. Активировать тему в разделе Appearance → Themes(Внешний → вид Темы).

## Ход работы

### Шаг 1. Подготовка среды

Перед началом разработки пользовательской темы `WordPress` необходимо подготовить рабочую среду и убедиться, что система `WordPress` установлена и функционирует корректно.

В данной лабораторной работе используется локальная серверная среда **XAMPP**, включающая веб-сервер **Apache** и систему управления базами данных **MySQL (MariaDB)**. Ранее установленная система WordPress используется как основа для разработки пользовательской темы.

Все темы `WordPress` хранятся в специальной директории проекта:

```
wp-content/themes
```

В этой папке располагаются как стандартные темы WordPress, так и пользовательские темы, создаваемые разработчиком.

Для выполнения лабораторной работы в данной директории была создана новая папка темы:

```
usm-theme
```

В дальнейшем в этой папке будут размещаться все файлы пользовательской темы `WordPress`.

![image](https://i.imgur.com/yUWcHtW.png)

Также для удобства разработки был включён режим отладки `WordPress`.

Для этого был открыт файл конфигурации системы:

```
wp-config.php
```

В данном файле был изменён параметр:

```php
define('WP_DEBUG', true);
```

Включение режима отладки позволяет отображать ошибки и предупреждения системы `WordPress`, что упрощает процесс разработки и тестирования темы.

![image](https://i.imgur.com/5VDaFmu.png)

### Шаг 2. Создание обязательных файлов темы

#### Структура папки темы

После создания директории пользовательской темы необходимо добавить в неё обязательные файлы, без которых `WordPress` не сможет распознать тему.

Минимальная тема WordPress должна содержать два файла:

* `style.css`
* `index.php`

Эти файлы являются обязательными для любой темы `WordPress`.

После создания файлов структура директории темы имеет следующий вид:

```
usm-theme
   style.css
   index.php
```

![image](https://i.imgur.com/EJJHMYn.png)

#### Создание файла `style.css`

В папке темы был создан файл:

```
style.css
```

В данный файл необходимо добавить **метаданные темы**, которые используются системой `WordPress` для отображения информации о теме в административной панели.

Пример метаданных темы:

```css
/*
Theme Name: USM Theme
Theme URI: http://localhost
Author: Student
Description: Простая тема WordPress для лабораторной работы
Version: 1.0
*/
```

После метаданных в файл необходимо добавить базовые CSS-правила для стилизации основных элементов сайта, таких как тело страницы, шапка и подвал.

![image](https://i.imgur.com/9LVpx1J.png)

#### Создание файла `index.php`

После этого был создан файл:

```
index.php
```

Данный файл является **главным шаблоном темы WordPress**.

Он используется системой для отображения содержимого сайта, если для конкретной страницы не существует специализированного шаблона.

В файл была добавлена базовая HTML-структура страницы, а также реализован **цикл WordPress**, который позволяет выводить записи сайта.

В шаблоне выводятся:

* название сайта
* описание сайта
* список последних записей

Для вывода записей используется стандартный цикл WordPress:

```php
if (have_posts()) :
    while (have_posts()) : the_post();
```

Этот механизм позволяет динамически получать записи из базы данных `WordPress` и отображать их на странице.

![image](https://i.imgur.com/0ucI57R.png)

### Шаг 3. Общие части шаблонов

На данном этапе необходимо выполнить разделение шаблонов темы на общие части. Такой подход используется в `WordPress` для упрощения структуры темы и повторного использования кода на различных страницах сайта.

#### Создание файла `header.php`

В папке темы необходимо создать файл:

```
header.php
```

В данный файл необходимо перенести код шапки сайта, который располагается до начала основного содержимого страницы.

Файл должен содержать:

* объявление HTML-документа;
* блок `<head>`;
* вывод названия сайта;
* вывод описания сайта;
* открытие основного блока страницы.

Для корректной работы темы необходимо использовать функцию `WordPress`:

```php
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title><?php bloginfo('name'); ?></title>
<?php wp_head(); ?>
</head>

<body>

<header>
<h1><?php bloginfo('name'); ?></h1>
<p><?php bloginfo('description'); ?></p>
</header>

<main>
```

Функция `wp_head()` позволяет `WordPress` подключать необходимые стили, скрипты и другие элементы.

![image](https://i.imgur.com/30Tp26J.png)

#### Создание файла `footer.php`

Далее необходимо создать файл:

```
footer.php
```

В данный файл необходимо перенести код подвала сайта, который располагается после основного содержимого страницы.

Файл содержит закрытие блока `<main>`, подвал сайта и функцию WordPress `wp_footer()`.

Пример содержимого файла:

```php
</main>

<footer>
<p>© <?php echo date('Y'); ?> My WordPress Theme</p>
</footer>

<?php wp_footer(); ?>

</body>
</html>
```

Функция `wp_footer()` используется системой WordPress для подключения скриптов и других элементов перед закрытием страницы.

![image](https://i.imgur.com/XwndftB.png)

#### Подключение шаблонов в `index.php`

После создания файлов `header.php` и `footer.php` необходимо подключить их в основном шаблоне `index.php`.

Для подключения шапки используется функция:

```php
<?php get_header(); ?>
```

Для подключения подвала используется функция:

```php
<?php get_footer(); ?>
```

Данные функции автоматически подключают соответствующие файлы шаблонов темы.

#### Вывод последних записей

На главной странице необходимо вывести список последних записей сайта. Для этого используется стандартный цикл WordPress.

В файле `index.php` необходимо реализовать следующий код:

```php
<?php get_header(); ?>

<h2>Последние записи</h2>

<?php
$count = 0;

if (have_posts()) :
    while (have_posts()) : the_post();

    if ($count == 5) break;
?>

<h3><?php the_title(); ?></h3>
<p><?php the_excerpt(); ?></p>

<?php
$count++;
    endwhile;
endif;
?>

<?php get_footer(); ?>
```

В процессе выполнения цикла выводятся:

* заголовок записи (`the_title()`);
* краткое описание записи (`the_excerpt()`).

С помощью переменной `$count` ограничивается количество выводимых записей, поэтому на главной странице отображаются **пять последних публикаций**.

![image](https://i.imgur.com/icqYGG2.png)

### Шаг 4. Файл функций

На данном этапе необходимо создать файл функций темы `WordPress`.

Файл `functions.php` используется для добавления дополнительной функциональности теме. В данном файле можно подключать стили и скрипты, регистрировать меню, виджеты и добавлять различные возможности темы.

#### Создание файла `functions.php`

В папке темы необходимо создать файл:

```
functions.php
```

Данный файл автоматически загружается системой `WordPress` при активации темы и позволяет добавлять пользовательские функции.

#### Подключение стилей темы

Для корректного подключения стилей темы необходимо использовать функцию `WordPress` `wp_enqueue_style()`.

В файл `functions.php` необходимо добавить следующий код:

```php
<?php

function usm_theme_styles() {

    wp_enqueue_style(
        'usm-style',
        get_stylesheet_uri()
    );

}

add_action('wp_enqueue_scripts', 'usm_theme_styles');

?>
```

![image](https://i.imgur.com/SNFj0RX.png)

Функция `wp_enqueue_style()` используется для подключения CSS-файлов темы.

Функция `get_stylesheet_uri()` возвращает путь к файлу `style.css`, который находится в корневой папке темы.

Для выполнения данной функции используется хук `WordPress`:

```php
add_action('wp_enqueue_scripts', 'usm_theme_styles');
```

Данный хук сообщает системе `WordPress` о необходимости выполнить функцию подключения стилей при загрузке страницы.

#### Проверка структуры темы

После создания файла `functions.php` структура директории темы имеет следующий вид:

```
usm-theme
   style.css
   index.php
   header.php
   footer.php
   functions.php
```

### Шаг 5. Дополнительные шаблоны

На данном этапе необходимо создать дополнительные шаблоны темы `WordPress`. Эти шаблоны используются системой для отображения различных типов страниц сайта.

В рамках данного шага необходимо создать следующие файлы:

```
single.php
page.php
sidebar.php
comments.php
archive.php
```

Каждый из этих файлов отвечает за отображение определённого типа страниц сайта.

#### Создание файла `single.php`

Для отображения отдельной записи необходимо создать файл:

```
single.php
```

Данный шаблон используется системой `WordPress` для отображения страницы отдельного поста.

Пример содержимого файла:

```php
<?php get_header(); ?>

<h2>Запись</h2>

<?php
if (have_posts()) :
    while (have_posts()) : the_post();
?>

<h3><?php the_title(); ?></h3>
<p><?php the_content(); ?></p>

<?php get_sidebar(); ?>

<?php comments_template(); ?>

<?php
    endwhile;
endif;
?>

<?php get_footer(); ?>
```

В данном коде:

* `get_header()` подключает файл **header.php**;
* `have_posts()` и `the_post()` реализуют стандартный **WordPress Loop**;
* `the_title()` выводит заголовок записи;
* `the_content()` выводит содержимое записи;
* `get_sidebar()` подключает боковую панель сайта;
* `comments_template()` подключает шаблон комментариев;
* `get_footer()` подключает файл **footer.php**.

![image](https://i.imgur.com/oGJkadE.png)

#### Создание файла `page.php`

Для отображения страниц сайта необходимо создать файл:

```
page.php
```

Данный шаблон используется системой `WordPress` для вывода статических страниц сайта.

Пример содержимого файла:

```php
<?php get_header(); ?>

<h2>Страница</h2>

<?php
if (have_posts()) :
    while (have_posts()) : the_post();
?>

<h3><?php the_title(); ?></h3>
<p><?php the_content(); ?></p>

<?php get_sidebar(); ?>

<?php comments_template(); ?>

<?php
    endwhile;
endif;
?>

<?php get_footer(); ?>
```

В данном шаблоне используется тот же цикл `WordPress` для вывода содержимого страницы.

![image](https://i.imgur.com/WsJxMFU.png)

#### Создание файла `sidebar.php`

Для отображения боковой панели необходимо создать файл:

```
sidebar.php
```

Пример содержимого файла:

```php
<aside>
<h3>Боковая панель</h3>

<ul>
<li>Главная страница</li>
<li>Страницы сайта</li>
<li>Записи блога</li>
</ul>

</aside>
```

Данный файл используется для отображения боковой панели сайта. Подключение панели выполняется с помощью функции:

```php
<?php get_sidebar(); ?>
```

![image](https://i.imgur.com/WTiwZbk.png)

#### Создание файла `comments.php`

Для отображения комментариев необходимо создать файл:

```
comments.php
```

Пример содержимого файла:

```php
<?php
if (have_comments()) :
?>

<h3>Комментарии</h3>

<?php
wp_list_comments();
endif;

if (comments_open()) :
    comment_form();
endif;
?>
```

Здесь:

* `have_comments()` проверяет наличие комментариев;
* `wp_list_comments()` выводит список комментариев;
* `comments_open()` проверяет, разрешены ли комментарии;
* `comment_form()` выводит форму добавления комментария.

Файл подключается в шаблонах с помощью функции:

```php
<?php comments_template(); ?>
```

![image](https://i.imgur.com/ppWOZtr.png)

#### Создание файла `archive.php`

Для отображения архивов записей необходимо создать файл:

```
archive.php
```

Данный шаблон используется `WordPress` для отображения архивов записей (например, архивов по категориям или датам).

Пример содержимого файла:

```php
<?php get_header(); ?>

<h2>Архив записей</h2>

<?php
if (have_posts()) :
    while (have_posts()) : the_post();
?>

<h3><?php the_title(); ?></h3>
<p><?php the_excerpt(); ?></p>

<?php
    endwhile;
endif;
?>

<?php get_sidebar(); ?>

<?php get_footer(); ?>
```

В данном коде:

* используется стандартный **WordPress Loop**;
* `the_title()` выводит заголовок записи;
* `the_excerpt()` выводит краткое описание записи;
* `get_sidebar()` подключает боковую панель сайта.

![image](https://i.imgur.com/ga3zgSV.png)

### Шаг 6. Стилизация темы

На данном этапе необходимо добавить стили для основных элементов темы `WordPress`.
Стилизация позволяет улучшить внешний вид сайта и сделать его структуру более понятной для пользователя.

В рамках данного шага необходимо оформить следующие элементы темы:

* шапку сайта;
* подвал сайта;
* основной контент;
* боковую панель.

Все стили добавляются в файл:

```css
style.css
```

#### Добавление стилей для основных элементов темы

В файл `style.css` необходимо добавить CSS-правила для оформления основных блоков страницы.

Пример содержимого файла:

```css
/*
Theme Name: USM Theme
Theme URI: http://localhost
Author: Student
Description: Простая тема WordPress для лабораторной работы
Version: 1.0
*/

body{
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background: #f4f4f4;
}

header{
    background: #333;
    color: white;
    padding: 20px;
    text-align: center;
}

main{
    padding: 20px;
    background: white;
    width: 70%;
    float: left;
    box-sizing: border-box;
}

aside{
    width: 30%;
    float: left;
    background: #eaeaea;
    padding: 20px;
    box-sizing: border-box;
}

footer{
    background: #222;
    color: white;
    text-align: center;
    padding: 20px;
    clear: both;
}
```

В данном коде:

* для элемента `body` задаются шрифт, отступы и цвет фона страницы;
* для `header` задаются цвет фона, цвет текста, внутренние отступы и выравнивание по центру;
* для `main` задаются отступы, белый фон и ширина основного содержимого;
* для `aside` задаются ширина, фон и отступы боковой панели;
* для `footer` задаются цвет фона, цвет текста, внутренние отступы и очистка обтекания.

Таким образом создаётся простое визуальное разделение основных частей сайта.

### Шаг 7. Скриншот темы

Для корректного отображения темы в административной панели `WordPress` необходимо добавить изображение-превью темы.

В папке пользовательской темы создаётся файл:

```
screenshot.png
```

Данный файл используется системой WordPress для отображения **предварительного просмотра темы** в разделе управления темами.

Файл необходимо разместить непосредственно в директории темы.

Структура папки темы после добавления изображения имеет следующий вид:

```
usm-theme
   archive.php
   comments.php
   footer.php
   functions.php
   header.php
   index.php
   page.php
   sidebar.php
   single.php
   style.css
   screenshot.png
```

![image](https://i.imgur.com/EbQtHOD.png)

#### Шаг 8. Активация темы

После создания файлов темы и добавления изображения-превью необходимо активировать тему в системе WordPress.

Для этого в административной панели необходимо перейти в раздел:

```
Appearance → Themes
```

В списке доступных тем отображается созданная тема **USM Theme** с добавленным изображением-превью.

После этого необходимо нажать кнопку **Activate**, чтобы активировать созданную тему.

После активации `WordPress` начинает использовать шаблоны данной темы для отображения сайта.

![image](https://i.imgur.com/KoFmBWi.png)

Далее необходимо открыть главную страницу сайта и убедиться, что сайт отображается с использованием созданной темы.

![image](https://i.imgur.com/YuRR5bW.png)

## Контрольные вопросы

### 1. Какие два файла являются обязательными для любой темы WordPress?

Для того чтобы WordPress распознал пользовательскую тему, она должна содержать как минимум два обязательных файла.

Первый файл — **style.css**.
В этом файле содержатся метаданные темы (название темы, автор, описание, версия). WordPress считывает эту информацию и отображает тему в административной панели.

Второй файл — **index.php**.
Этот файл является основным шаблоном темы. Он используется системой для вывода содержимого сайта, если для страницы не существует более специализированного шаблона.

### 2. Как подключаются общие части шаблонов (header, footer, sidebar)?

В WordPress общие части шаблонов (шапка сайта, подвал и боковая панель) подключаются с помощью специальных функций.

Для подключения шапки сайта используется функция:

```
get_header();
```

Для подключения подвала сайта используется функция:

```
get_footer();
```

Для подключения боковой панели используется функция:

```
get_sidebar();
```

Эти функции позволяют разделить структуру сайта на отдельные файлы шаблонов и использовать их повторно в разных страницах темы.

Например, файл **header.php** подключается в шаблонах страниц, чтобы отобразить верхнюю часть сайта, а файл **footer.php** — для отображения нижней части сайта.

### 3. Чем отличаются index.php, single.php и page.php?

В WordPress разные типы страниц могут использовать разные шаблоны.

Файл **index.php** является главным шаблоном темы. Он используется системой для отображения страниц сайта, если для них не существует более специализированных шаблонов.

Файл **single.php** используется для отображения отдельной записи блога. Когда пользователь открывает конкретную запись, WordPress использует именно этот шаблон.

Файл **page.php** используется для отображения статических страниц сайта, например страниц «О нас», «Контакты» или других информационных страниц.

### 4. Зачем нужен файл functions.php в теме?

Файл **functions.php** используется для добавления функциональности теме WordPress.

В этом файле можно подключать стили и скрипты, регистрировать меню сайта, добавлять поддержку различных возможностей WordPress и расширять функциональность темы.

Например, в файле **functions.php** можно подключить файл стилей темы с помощью функции:

```
wp_enqueue_style()
```

Также в этом файле можно регистрировать области виджетов, добавлять поддержку миниатюр записей, настраивать меню навигации и выполнять другие действия, связанные с работой темы.

Файл **functions.php** позволяет расширять возможности темы и управлять её функциональностью.

## Выводы

В ходе выполнения лабораторной работы были изучены основные принципы создания пользовательской темы для системы управления контентом WordPress.

В процессе работы была создана собственная тема сайта, включающая обязательные файлы `style.css` и `index.php`, а также дополнительные шаблоны, такие как `header.php`, `footer.php`, `sidebar.php`, `single.php`, `page.php`, `archive.php` и `comments.php`.

Также был создан файл `functions.php`, который используется для подключения стилей темы и добавления функциональности. Для корректного отображения темы в административной панели был добавлен файл `screenshot.png`, содержащий изображение-превью темы.

После создания всех необходимых файлов тема была успешно активирована в административной панели WordPress и использована для отображения содержимого сайта.

Таким образом, в ходе выполнения лабораторной работы были получены практические навыки создания и настройки пользовательских тем в WordPress, а также изучена структура файлов темы и принципы работы шаблонов системы управления контентом.

## Библиография

1. [WordPress Documentation](https://wordpress.org/documentation/) — официальная документация по системе управления контентом **WordPress**.
2. [Theme Developer Handbook](https://developer.wordpress.org/themes/) — руководство по разработке пользовательских **тем WordPress**, включая структуру файлов темы и использование шаблонов.
3. [Template Hierarchy](https://developer.wordpress.org/themes/basics/template-hierarchy/) — описание иерархии шаблонов WordPress, объясняющее работу файлов `index.php`, `single.php`, `page.php` и других шаблонов темы.
4. [WordPress Codex](https://codex.wordpress.org/) — справочник по функциям, структуре тем и возможностям системы управления контентом **WordPress**.
5. [Introduction to WordPress Themes](https://developer.wordpress.org/themes/getting-started/) — введение в разработку тем WordPress и объяснение основных принципов работы тем.
